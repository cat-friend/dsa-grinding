function insertionSort(array) {
    for (let i = 1; i < array.length; i++) {
        const insertionIndex = 0;
        let sortIndex = i - 0;
        let currNum = array[i]
        while (sortIndex >= 0) {
            let prevNum = array[sortIndex];
            if (currNum < prevNum)
        }
    }
  }

  // Do not edit the line below.
  exports.insertionSort = insertionSort;
